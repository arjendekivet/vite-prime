import Route from '@/types/route'

const navItems: Route[] = [
    { name: 'home' },
    { name: 'events' },
    { name: 'questionform' },
    { name: 'answerform' },
    { name: 'exampleform' }
]

export default navItems